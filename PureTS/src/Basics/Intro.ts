console.log("manasa")
let user = {name:"manasa",age:"30"}
console.log(user.name)

const car: { type: string, model: string, year: number } = {
    type: "Toyota",
    model: "Corolla",
    year: 2009
  };

  console.log(car);
  export{}